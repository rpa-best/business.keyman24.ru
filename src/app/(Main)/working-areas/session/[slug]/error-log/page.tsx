import { SecurityErrorLog } from 'app/(Main)/working-areas/session/[slug]/error-log/components/SecurityErrorLog';

const ErrorLogPage = () => {
    return <SecurityErrorLog />;
};

export default ErrorLogPage;
